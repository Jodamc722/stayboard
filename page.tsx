// STRAY COPY. app/command/page.tsx was uploaded into the wrong folder on 2026-09-02 ("Add files via
// upload" 30aff3c). It is not routable and never was. Replaced with this stub so the build stays
// green; DELETE this file (it was listed in the 2026-09-02 SHIP-NOTES).
export {}
